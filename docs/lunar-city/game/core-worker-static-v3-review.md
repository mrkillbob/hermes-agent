# Core worker static v3 review

This is a preserved partial finishing study, not an accepted production character.

- Source geometry remains in the original workshop scenes and v2 study.
- Local helmet cleanup welds coincident seams; the roof gear and visor colors are baked for portable export.
- The distal fused probe region was replaced with three separate fingers and a cyan-tipped stylus.
- The detached visor experiment is hidden and excluded from the export.
- Export was reimported into Blender and rendered: 24 meshes, 1.200000 m overall height, ground minimum zero. Front, rear, top and overview were visually checked; the visor repair does not create a rear face.
- No rig is included. Existing animated runtime assets remain selected.

Still required: body panel reconstruction, tablet/screen cleanup, wheel/fender interfaces, arm/socket cleanup, and integration of the finished parts into the mechanical rig. The prior rim/tire animation issue is not accepted by this static review.

Assets: `apps/desktop/public/lunar-city/worker-multiview-2026-09-07/core-runtime-ux-repairs/static-finish-v3/`. `complete.json` and `reimport.json` identify the exact GLB hash. The visible Blender workshop contains `Worker static finish | core v3 probe` and `Worker GLB verification | core v3`.
